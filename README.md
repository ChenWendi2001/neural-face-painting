## Getting started :hugs:

1. `python inference.py `

   This generates `strokes.pkl`, which contains a list of parameters of different layers.

2. `render.py`

   `render()` takes into  `strokes` and returns the final image.

